services:
  - type: web
    name: agrointel
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: agrointel-db
          property: connectionString

databases:
  - name: agrointel-db
    plan: free